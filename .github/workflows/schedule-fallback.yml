name: Schedule Update Fallback
on:
  schedule:
    - cron: "15 5 * * *"
  workflow_dispatch:
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS -f -m 90 https://day-rating.vercel.app/api/schedule_update
